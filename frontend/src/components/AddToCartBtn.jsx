export default function AddToCartBtn({ data }) {
  return (
    <div>
      <button
        onClick={() => {}}
        className="bg-primary-200 hover:bg-primary-100 text-white px-2 lg:px-4 py-1 rounded"
      >
        Add
      </button>
    </div>
  );
}
